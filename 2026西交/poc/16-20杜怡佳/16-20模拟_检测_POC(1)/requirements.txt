# ============================================
# 工控协议检测与分析系统 — 依赖清单
# 协议 16-20: FOCAS, ADS, GOOSE, SV, TRDP
# ============================================
# 核心依赖（必须安装）
scapy>=2.5.0            # 数据包构造/解析 (GOOSE/SV 使用 scapy 协议层)
pyads>=3.4.0            # Beckhoff TwinCAT ADS 协议 (pip 安装, Windows 需要 TwincAT 运行时或测试服务器)

# 可选增强（协议特定）
# FOCAS: 需要 FANUC 官方 fwlib32.dll / libfwlib32.so (专有软件, 非 pip)
#   替代纯 Python 方案: pyfanuc (pip install pyfanuc, 但有 fwlipy 依赖问题)
#                       fanucpy (pip install fanucpy, 支持 FANUC 机器人)

# TRDP: 暂无纯 Python 库, 基于 IEC 61375 规范手动实现

# 开发/测试辅助
pyshark>=0.6             # Wireshark/TShark 集成 (可选)
pycryptodome>=3.19       # 加解密支持 (GOOSE/SV 签名验证)
